#include <stdio.h>

int main() {
    int firstNum;
    int secondNum;
    scanf("%d %d", &firstNum, &secondNum);
    printf("Your first number is %d. Your second number is %d.", firstNum, secondNum);
}